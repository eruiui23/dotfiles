# Variabel
set $mod Mod4


exec_always --no-startup-id stalonetray --geometry 3x1+980+0 --dockapp-mode simple --window-class "StalonetrayToggle"
# Aturan untuk Stalonetray
for_window [class="StalonetrayToggle"] floating enable, border pixel 0, sticky enable, no_focus, move to workspace number 9
# exec --no-startup-id bash -c "mpv --fs --no-border --volume=100 --really-quiet Videos/chisa.mp4" 
# export QT_QPA_PLATFORMTHEME=gtk3
# export QT_QPA_PLATFORMTHEME=qt5ct   # (untuk Qt5)
# export QT_QPA_PLATFORMTHEME=qt6ct   # (untuk Qt6)

# exec_always --no-startup-id export QT_QPA_PLATFORMTHEME=qt6ct
# exec_always --no-startup-id export QT_FILE_SELECTORS=gtk 

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
# font pango:monospace 8

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
# font pango:DejaVu Sans Mono 8
font pango:JetBrains Mono 10
# font pango:JetBrains Mono 12



# Start XDG autostart .desktop files using dex. See also
# https://wiki.archlinux.org/index.php/XDG_Autostart
exec --no-startup-id dex --autostart --environment i3

# The combination of xss-lock, nm-applet and pactl is a popular choice, so
# they are included here as an example. Modify as you see fit.

# xss-lock grabs a logind suspend inhibit lock and will use i3lock to lock the
# screen before suspend. Use loginctl lock-session to lock your screen.
# exec --no-startup-id xss-lock --transfer-sleep-lock -- i3lock --nofork
exec --no-startup-id xss-lock --transfer-sleep-lock -- betterlockscreen -l --nofork

# NetworkManager is the most popular way to manage wireless networks on Linux,
# and nm-applet is a desktop environment-independent system tray GUI for it.
exec --no-startup-id nm-applet

exec --no-startup-id ffplay -nodisp -autoexit Music/chisa.mp3          

# Load nitrogen wallpaper
# exec --no-startup-id nitrogen --restore
exec_always --no-startup-id sleep 0.5 && nitrogen --restore


# Use pactl to adjust volume in PulseAudio.
set $refresh_i3status killall -SIGUSR1 i3status
bindsym XF86AudioRaiseVolume exec --no-startup-id exec ~/scripts/volume-osd.sh up
bindsym XF86AudioLowerVolume exec --no-startup-id exec ~/scripts/volume-osd.sh down

# bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -10% && $refresh_i3status
# bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +10% && $refresh_i3status
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $refresh_i3status
# bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $refresh_i3status

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# move tiling windows via drag & drop by left-clicking into the title bar,
# or left-clicking anywhere into the window while holding the floating modifier.
tiling_drag modifier titlebar

# start a terminal
# bindsym $mod+Return exec i3-sensible-terminal
bindsym $mod+Return exec --no-startup-id kitty -1
# bindsym $mod+Shift+Return exec --no-startup-id i3-msg 'exec kitty; floating enable' 
bindsym $mod+Shift+Return exec --no-startup-id kitty --title="nara230"



for_window [class="kitty"] move position center
# kill focused window
bindsym $mod+Escape kill

# ----------------------------------------------------- Rofi
# pake convert -strip input.png output.png buat bikin imagenya load cepet
bindsym $mod+space exec --no-startup-id ~/.config/rofi/launchers/type-6/launcher.sh
# bindsym $mod+space exec --no-startup-id ~/.config/rofi/launchers/type-6/launcher_window.sh
bindsym $mod+p exec --no-startup-id /home/yucandy16/.config/rofi/powermenu/type-6/powermenu.sh

# alt tab
bindsym $mod+Tab workspace back_and_forth
# bindsym $mod+control+l workspace next
# bindsym $mod+control+h workspace prev
# bindsym mod2+h workspace prev
# bindsym mod1+l workspace next
bindsym $mod+Right workspace next
bindsym $mod+Left workspace prev


# A more modern dmenu replacement is rofi:
# bindcode $mod+40 exec "rofi -modi drun,run -show drun"
# There also is i3-dmenu-desktop which only displays applications shipping a
# .desktop file. It is a wrapper around dmenu, so you need that installed.
# bindcode $mod+40 exec --no-startup-id i3-dmenu-desktop

# change focus vim mode
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right
bindsym mod1+Tab focus right
bindsym mod1+Shift+Tab focus left
bindsym $mod+h focus left

# bindsym $mod+j focus left
# bindsym $mod+k focus down
# bindsym $mod+l focus up
# bindsym $mod+semicolon focus right

# alternatively, you can use the cursor keys:
# bindsym $mod+Left focus left
# bindsym $mod+Down focus down
# bindsym $mod+Up focus up
# bindsym $mod+Right focus right

# move focused window
set $pixel 50px
bindsym $mod+Shift+j move down $pixel
bindsym $mod+Shift+k move up $pixel
bindsym $mod+Shift+l move right $pixel
bindsym $mod+Shift+h move left $pixel

# bindsym $mod+Shift+j move left
# bindsym $mod+Shift+k move down
# bindsym $mod+Shift+l move up
# bindsym $mod+Shift+semicolon move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+b split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s floating toggle
bindsym $mod+o sticky toggle
bindsym $mod+w layout tabbed
# bindsym $mod+w exec "i3-msg focus parent && i3-msg layout tabbed"
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+s focus mode_toggle


# focus the parent container
bindsym $mod+a focus parent

# focus the child container
bindsym $mod+z focus child

# Define names for default workspaces for which we configure key bindings later on.
# We use variables to avoid repeating the names in multiple places.
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"
set $ws11 "11"
set $ws12 "12"
set $ws13 "13"

# set $ws1 "一"
# set $ws2 "二"
# set $ws3 "三"
# set $ws4 "四"
# set $ws5 "五"
# set $ws6 "六"
# set $ws7 "七"
# set $ws8 "八"
# set $ws9 "九"
# set $ws10 "十"

# switch to workspace (number)
bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws6
bindsym $mod+7 workspace number $ws7
bindsym $mod+8 workspace number $ws8
bindsym $mod+9 workspace number $ws9
bindsym $mod+0 workspace number $ws10
bindsym $mod+control+1 workspace number $ws11
bindsym $mod+control+2 workspace number $ws12
bindsym $mod+control+3 workspace number $ws13

# switch to workspace (word)
# bindsym $mod+z workspace number $ws1
# bindsym $mod+x workspace number $ws2
# bindsym $mod+c workspace number $ws3
# bindsym $mod+a workspace number $ws4
# bindsym $mod+s workspace number $ws5
# bindsym $mod+d workspace number $ws6
# bindsym $mod+q workspace number $ws7
# bindsym $mod+w workspace number $ws8
# bindsym $mod+e workspace number $ws9
# bindsym $mod+control+z workspace number $ws10

# workspace_auto_back_and_forth yes 
# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace number $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6
bindsym $mod+Shift+7 move container to workspace number $ws7
bindsym $mod+Shift+8 move container to workspace number $ws8
bindsym $mod+Shift+9 move container to workspace number $ws9
bindsym $mod+Shift+0 move container to workspace number $ws10
bindsym $mod+control+Shift+1 move container to workspace number $ws11
bindsym $mod+control+Shift+2 move container to workspace number $ws12
bindsym $mod+control+Shift+3 move container to workspace number $ws13

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
# bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -B 'Yes, exit i3' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym h resize shrink width 10 px or 10 ppt
        bindsym k resize grow height 10 px or 10 ppt
        bindsym j resize shrink height 10 px or 10 ppt
        bindsym l resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape or $mod+r
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+r mode "default"
}

bindsym $mod+r mode "resize"

# COLOR
# client.focused          #14282f #14282f #e8e8e8 #2cdeb4 #347568
client.focused          #14282f #14282f #e8e8e8 #14282f #14282f
client.unfocused       #14282f #222222 #676767 #14282f #14282f
client.focused_inactive #14282f #14282f #676767 #14282f #14282f 
hide_edge_borders smart 

# Start i3bar to display a workspace bar (plus the system information i3statuss
# finds out, if available)
# bar {
#        status_command i3status
#} hehe

# -------------------------------------- border
title_align center # buat bikin judulnya di tengah
# for_window [class="^.*"] border pixel 4.5
# for_window [class="^.*"] border none
for_window [class="^.*"] border normal 0 
# for_window [class="kitty"] border pixel 5 
# for_window [class=""] border none 
# default_border normal 0 
for_window [all] border pixel 0
# for_window [all] title_format "%class %title"
for_window [class="Anki"] title_format "%title"
for_window [class="kitty"] title_format "%title"
for_window [class="Code"] border pixel 0
for_window [class="obsidian"] border pixel 0
for_window [class="zen"] border pixel 0
for_window [class="kitty"] border pixel 0
# for_window [all] title_window_icon padding 3px

# Load polybar
exec_always --no-startup-id sh -c "pkill polybar; polybar --reload -c $HOME/.config/polybar/config.ini &"

# ---------------------------------------- Set refresh rate sec monitor
exec_always --no-startup-id xrandr  --output HDMI-1-0 --mode 1920x1080 --rate 180.00 

# gaps
# Inner gaps for all windows: space between two adjacent windows (or split containers).
gaps inner 8px 
# gaps outer 8px 
bindsym $mod+g exec --no-startup-id ~/scripts/gaps.sh


# set touchpad biar ga kebalik
exec --no-startup-id xinput set-prop "SYNA2BA6:00 06CB:CE2D Touchpad" "libinput Natural Scrolling Enabled" 1
exec_always --no-startup-id xinput set-prop "SYNA2BA6:00 06CB:CE2D Touchpad" "libinput Accel Speed" 0.3 


# open picom
exec_always --no-startup-id picom --experimental-backends --config ~/.config/picom/picom.conf


# open app, browser and file
# bindsym $mod+u  --no-startup-id i3-msg "workspace $ws3"; exec sleep 5 && flatpak run app.zen_browser.zen

bindsym $mod+u exec --no-startup-id /home/yucandy16/.local/share/zen/zen
bindsym $mod+y exec --no-startup-id nemo
bindsym $mod+i exec --no-startup-id spotify
# bindsym $mod+c exec --no-startup-id code
# bindsym $mod+o exec anki; exec sleep 5; exec sh -c 'XAPP_FORCE_GTKWINDOW_ICON="/home/yucandy16/Downloads/jisho.png" firefox --class WebApp-Jisho6611 --name WebApp-Jisho6611 --profile /home/yucandy16/.local/share/ice/firefox/Jisho6611 --no-remote "https://jisho.org/"'

# bindsym $mod+shift+o exec ~/scripts/start.sh  
# ----------------------------------------- Startup script
exec --no-startup-id ~/scripts/start.sh  

# Assign Workspaces:
assign [class="zen"] $ws3
assign [class="Code"] $ws4
assign [class="Spotify"] $ws10
# assign [class="WebApp-Jisho6611"] $ws8
# assign [class="Anki"] $ws8
assign [class="Nemo"] $ws2
assign [class="discord"] $ws9
assign [class="WebApp-whatsapp2493"] $ws9
assign [class="obsidian"] $ws5

# -------------------------------------- window setting (floating, position, etc)
# for_window [class="Galendae"] floating enable, resize set 600 400, move absolute position 660 45
for_window [class="zoom"] floating enable, move position center
for_window [class="Xviewer"] floating enable
for_window [class="Anki"] floating enable, move position center
for_window [class="Cheese"] floating enable, resize set 1200 800, move position center
for_window [class="Nitrogen"] floating enable, resize set  800 600, move position center
for_window [class="MintUpdate.py"] floating enable, resize set  1200 800, move position center
for_window [class="Drawing"] floating enable, resize set  800 600, move position center
for_window [title="xzoom"] floating enable, resize set  800 600, move position center
for_window [class="fcitx5-config-qt"] floating enable, resize set  800 600, move position center
for_window [class="Pavucontrol"] floating enable, move position center, resize set 800 600
for_window [title="nara230"] floating enable, resize set  800 600, move position center
for_window [title="i3switcherX11"] floating enable
for_window [title="Picture-in-Picture"] stick enable
# exec --no-startup-id i3-switcher-x11
## -------------------------------------- screenshoot
bindsym Print exec --no-startup-id flameshot screen 
bindsym Control+Print exec --no-startup-id flameshot gui
bindsym mod1+z exec --no-startup-id /home/yucandy16/scripts/flameshotfocus.sh
# bindsym Print exec --no-startup-id bash -c "focused_window_id=$(xdotool getactivewindow); flameshot gui; sleep 0.2; xdotool windowactivate $focused_window_id"
exec --no-startup-id parcellite &
exec --no-startup-id flameshot &
# exec --no-startup-id dunst &
exec_always --no-startup-id dunst -conf ~/.config/dunst/dunstrc 

# --------------------------------------- xzoom (magnify tool)
bindsym $mod+m exec --no-startup-id xzoom -mag 2


#brightness
# Increase brightness
bindsym $mod+F6 exec --no-startup-id brightnessctl set +5%

# Decrease brightness
bindsym $mod+F5 exec --no-startup-id brightnessctl set 5%-


# -------------------------------- autotiling
# exec_always --no-startup-id /home/yucandy16/i3-alternating-layout/alternating_layouts.py

exec --no-startup-id xapp-status-applet

# Start Fcitx japanese
exec --no-startup-id fcitx5 -d

# set keyboard repeat delay and rate
exec_always --no-startup-id xset r rate 250 40

# login buat timeshift dan teman teman
exec --no-startup-id /usr/lib/policykit-1-gnome/polkit-gnome-authentication-agent-1


# ------------------------ Keybind ------------------------------#

# set capslock to ctrl
exec --no-startup-id setxkbmap -option ctrl:nocaps
# set right shift to meta/super
exec_always --no-startup-id xmodmap -e "keycode 62 = Super_R" -e "remove shift = Shift_R" -e "add mod4 = Super_R"


exec --no-startup-id caffeine

exec --no-startup-id syncthing





